{
  "name": "User",
  "type": "object",
  "properties": {
    "subscription_status": {
      "type": "string",
      "enum": [
        "free",
        "premium",
        "lifetime_vip",
        "cancelled"
      ],
      "default": "free",
      "description": "User's subscription status"
    },
    "subscription_type": {
      "type": "string",
      "enum": [
        "none",
        "monthly",
        "lifetime"
      ],
      "default": "none",
      "description": "Type of subscription - auto-set to 'lifetime' for VIP"
    },
    "subscription_start_date": {
      "type": "string",
      "format": "date-time",
      "description": "ONLY for monthly premium - leave BLANK for lifetime VIP"
    },
    "subscription_end_date": {
      "type": "string",
      "format": "date-time",
      "description": "ONLY for cancelled monthly - leave BLANK for lifetime VIP"
    },
    "vip_member": {
      "type": "boolean",
      "default": false,
      "description": "Check this for first 100 lifetime users (no dates needed)"
    },
    "vip_spot_number": {
      "type": "number",
      "description": "1-100 for lifetime VIP members only"
    }
  }
}